<% provide(:title, 'Person Listing') %>
<h1>Person Listing</h1>
<h2>Click for details</h2>
<%= link_to "Add new entry", new_person_path %>
<div class="list-group" style="width:30%">
  <% @people.each do |person| %>
    <%= link_to person.name, person_path(person), class: "list-group-item" %>
  <% end %>
</ul>
